library(shiny)
library(plotly)

shinyUI(navbarPage('Drug Distribution from 2011 to 2015',
                   tabPanel('Test',
                            titlePanel('In progress'),
                            
                            sidebarLayout(
                              
                              sidebarPanel(
                                
                              ),
                              
                              mainPanel(
                                
                              )
                            )
                   ), 
                   
                   tabPanel('Test2',
                            
                            titlePanel('In progress'),
                            
                            sidebarLayout(
                              
                              sidebarPanel(
                                
                              ),
                              
                              mainPanel(
                                
                              )
                            )
                   )
))